# Bundler
.bundle
vendor
Gemfile.lock

# Jekyll
.jekyll-cache
.jekyll-metadata
_site

# RubyGems
*.gem

# CV LaTeX build artifacts (the compiled PDFs are published to assets/files/, not here).
# Note: cv/cv.tex.j2 (template) and cv/fonts/ are tracked; only generated files are ignored.
cv/cv.tex
cv/cv-kr.tex
cv/*.aux
cv/*.log
cv/*.out
cv/*.pdf

# Personal source documents (the CV is authored from _data, not the docx)
*.docx

# OS / editor
.DS_Store
.idea
.vscode

# Local-only dev/agent docs — kept out of the public repo (repo is public)
CLAUDE.md
AGENTS.md
CONTEXT.md
docs/
